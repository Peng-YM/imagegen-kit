name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        target: [x86_64, aarch64]
        exclude:
          - os: ubuntu-latest
            target: aarch64
        include:
          - os: ubuntu-latest
            target: x86_64
            rust_target: x86_64-unknown-linux-gnu
            artifact_name: imagegen-kit-linux-x86_64
          - os: macos-latest
            target: x86_64
            rust_target: x86_64-apple-darwin
            artifact_name: imagegen-kit-macos-x86_64
          - os: macos-latest
            target: aarch64
            rust_target: aarch64-apple-darwin
            artifact_name: imagegen-kit-macos-aarch64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --check

      - name: Run clippy
        run: cargo clippy -- -D warnings

      - name: Run tests
        run: cargo test --release

      - name: Build release
        run: cargo build --release --target ${{ matrix.rust_target }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: target/${{ matrix.rust_target }}/release/imagegen-kit${{ matrix.os == 'windows-latest' && '.exe' || '' }}

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Rename artifacts with platform suffix
        run: |
          if [ -d "imagegen-kit-linux-x86_64" ]; then
            mv imagegen-kit-linux-x86_64/imagegen-kit imagegen-kit-linux-x86_64/imagegen-kit-linux-x86_64
            chmod +x imagegen-kit-linux-x86_64/imagegen-kit-linux-x86_64
          fi
          if [ -d "imagegen-kit-macos-x86_64" ]; then
            mv imagegen-kit-macos-x86_64/imagegen-kit imagegen-kit-macos-x86_64/imagegen-kit-macos-x86_64
            chmod +x imagegen-kit-macos-x86_64/imagegen-kit-macos-x86_64
          fi
          if [ -d "imagegen-kit-macos-aarch64" ]; then
            mv imagegen-kit-macos-aarch64/imagegen-kit imagegen-kit-macos-aarch64/imagegen-kit-macos-aarch64
            chmod +x imagegen-kit-macos-aarch64/imagegen-kit-macos-aarch64
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            imagegen-kit-linux-x86_64/imagegen-kit-linux-x86_64
            imagegen-kit-macos-x86_64/imagegen-kit-macos-x86_64
            imagegen-kit-macos-aarch64/imagegen-kit-macos-aarch64
          generate_release_notes: true
